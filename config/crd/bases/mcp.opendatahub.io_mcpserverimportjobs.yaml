---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: mcpserverimportjobs.mcp.opendatahub.io
spec:
  group: mcp.opendatahub.io
  names:
    kind: McpServerImportJob
    listKind: McpServerImportJobList
    plural: mcpserverimportjobs
    singular: mcpserverimportjob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: McpServerImportJob is the Schema for the mcpserverimportjobs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: McpServerImportJobSpec defines the desired state of McpServerImportJob.
            properties:
              catalogRef:
                description: Reference to the MCP Catalog to import the servers into
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              registryUri:
                description: Reference to the MCP Registry to import the servers from
                type: string
            required:
            - catalogRef
            - registryUri
            type: object
          status:
            description: McpServerImportJobStatus defines the observed state of McpServerImportJob.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
