---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: mcpservercertjobs.mcp.opendatahub.io
spec:
  group: mcp.opendatahub.io
  names:
    kind: McpServerCertJob
    listKind: McpServerCertJobList
    plural: mcpservercertjobs
    singular: mcpservercertjob
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: McpServerCertJob is the Schema for the mcpservercertjobs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: McpServerCertJobSpec defines the desired state of McpServerCertJob.
            properties:
              serverRef:
                description: Reference to the McpServer to be certified
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
            type: object
          status:
            description: McpServerCertJobStatus defines the observed state of McpServerCertJob.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
